<UserControl x:Class="AnalyticsOverlay.AnalyticsOverlaySettingsPanelWpfUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="161*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Select camera: " Grid.ColumnSpan="1" Grid.Column="0" VerticalAlignment="Center"/>
        <Button x:Name="buttonCameraSelect" Content="Select Camera" Grid.ColumnSpan="1" Grid.Column="1" Height="50" Margin="0" VerticalAlignment="Center" HorizontalAlignment="Stretch" Click="OnSelectCamera"/>
    </Grid>
</UserControl>
