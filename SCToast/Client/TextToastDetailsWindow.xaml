<Window x:Class="SCToast.Client.TextToastDetailsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="300" SizeToContent="Height" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Title="Text Toast Details">
    <Grid Margin="10">
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Id:"/>
            <TextBlock x:Name="_idTextBlock"/>
            <TextBlock Text="Duration:" Margin="0,5,0,0"/>
            <ComboBox x:Name="_durationComboBox" DisplayMemberPath="DisplayText"/>
            <TextBlock Text="Icon:" Margin="0,5,0,0"/>
            <ComboBox x:Name="_iconComboBox" DisplayMemberPath="DisplayText"/>
            <TextBlock Text="Primary text:" Margin="0,5,0,0"/>
            <TextBox x:Name="_primaryTextTextBox"/>
            <TextBlock Text="Secondary text:" Margin="0,5,0,0"/>
            <TextBox x:Name="_secondaryTextTextBox"/>
            <TextBlock Text="Count Text:" Margin="0,5,0,0"/>
            <TextBox x:Name="_countTextTextBox"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="_cancelButton" Content="Cancel" Click="CancelButton_OnClick"/>
                <Button x:Name="_okButton" Content="OK" Click="OkButton_OnClick" Margin="5,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
