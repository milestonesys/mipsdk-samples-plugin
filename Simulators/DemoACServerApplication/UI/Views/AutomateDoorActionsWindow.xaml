<Window x:Class="DemoServerApplication.UI.Views.AutomateDoorActionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Automate Door Actions" Height="170" Width="300" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Grid Margin="10">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                   Text="Event frequency (Events/sec):"/>

        <TextBox Grid.Row="1"
                 Name="eventFrequency"
                 Text="{Binding EventFrequency, Mode=TwoWay, NotifyOnValidationError=True, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,5,0,0">
            
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=startStopButton, Path=Content, UpdateSourceTrigger=PropertyChanged}" Value="Stop">
                            <Setter Property="IsEnabled" Value="False"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>

        <CheckBox Grid.Row="2" 
                  IsChecked="{Binding LogActions}"
                  Content="Log actions to file"
                  Margin="0,5,0,0"/>
        
        <ProgressBar Grid.Row="3" 
                     Height="20" 
                     Margin="0,5,0,0"
                     IsIndeterminate="{Binding ProgressBarIndeterminateValue}"/>
        
        <StackPanel Grid.Row="4"
                    Margin="0,5,0,0"
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">

            <Button Content="{Binding StartStopButtonText}" 
                    Name="startStopButton"
                    Command="{Binding StartStopCommand}"
                    MinWidth="80" 
                    Margin="0,0,5,0"
                    VerticalAlignment="Top">
                
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=eventFrequency, Path=(Validation.HasError), UpdateSourceTrigger=PropertyChanged}" Value="True">
                                <Setter Property="IsEnabled" Value="False"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    MinWidth="80" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
